#include<stdio.h>
 int main()
{
    int i;

printf("enter  numbers: \n");
scanf("%d",&i);
if(i>99)
{

printf("the number is not two digit");
}
    else if(i==99)
    {
    printf("the numer is highest in 2 digits");
    }
    else{
        printf(" the number is only 2 digits");
    }
    }

return 0;
}

